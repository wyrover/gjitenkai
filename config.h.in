/* config.h, generated from config.h.in by CMake ${CMAKE_VERSION} */

#define PROJECT_NAME       "${PROJECT_NAME}"
#define GJITENKAI_DATADIR  "${SHARE_DIR}"
#define GJITENKAI_DICDIR   "${DICT_DIR}"
#define GJITENKAI_LOCALDIR "${LOCALE_DIR}"
